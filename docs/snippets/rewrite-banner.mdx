{/*
This snippet flags pages that still contain CLI / self-host content from
the pre-revamp docs and require a content rewrite. The page is wired
into the new IA but its body has not yet been updated to be
cloud-first, screenshot-led, and CLI-free per the documentation revamp.
*/}

<Note>
**Page rewrite pending.** Sections of this page still describe CLI
commands or self-host configuration that are no longer part of the
Oxygen Cloud surface. Treat them as historical reference until the
page is rewritten.
</Note>
