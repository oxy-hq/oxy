---
title: YAML schemas
description: One page per file extension Oxygen reads
type: reference
---

Oxygen stores every entity — agents, procedures, data apps, semantic
views — as a YAML file in your workspace. Reference pages on this site
document every field, type, default, and example for each file type.

<CardGroup cols={2}>
  <Card title="config.yml" href="/guide/reference/yaml/config" />
</CardGroup>

<Note>
Dedicated schema pages for `.agent.yml`, `.agentic.yml`, `.procedure.yml`,
`.app.yml`, `.view.yml`, and `.topic.yml` are being generated. Until then, the
[Build guides](/guide/build/agents) and the
[Semantic Layer](/guide/build/semantic-layer) docs cover these fields.
</Note>

{/* TODO: Auto-generate the schema pages above from the JSON Schema output. */}
