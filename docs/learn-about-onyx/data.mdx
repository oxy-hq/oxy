---
title: "Data"
description: "Raw semantic source material that your agents have access to"
---

The `data` directory stores SQL queries organized hierarchically by team scope. Place queries in directories based on their broadest user group - for example, company-wide queries go in the root `data` directory, while team-specific queries belong in their respective team subdirectories.
