---
title: Architecture
description: How Oxygen Cloud fits together at a glance
type: concept
audience: end-user
tier: standard
---

Oxygen Cloud has three surfaces a user interacts with:

- **`app.oxygen-hq.com`** — the IDE, chat panel, dashboards, and admin UI.
- **The REST API** — served by the same host under `app.oxygen-hq.com/api`,
  with a curated, API-key-only automation surface at `/external/api`. It
  powers automation and embedded integrations.
  ([REST API →](/guide/reference/rest-api))
- **The MCP server** — exposes Oxygen as a tool surface for MCP clients like
  Claude Desktop and Cursor, run from the `oxy mcp` CLI.
  ([MCP →](/guide/build/embed-extend/mcp))

{/* TODO: Annotated diagram. */}

Inside a workspace, Oxygen organizes work into:

- **Agents** that answer questions
- **Procedures** that automate multi-step workflows
- **Data Apps** that render dashboards
- A **Semantic Layer** that backs all of the above with shared models

Authentication, billing, and team-management live in **Manage**.
Configuration lives in workspace files that Oxygen edits for you
through the Builder agent (you can also edit them by hand in the IDE).
