name: survey_responses
steps:
  - name: weekly_metrics
    type: execute_sql
    warehouse: primary_warehouse
    sql_file: default/example.sql
  - name: monthly_report
    type: agent
    prompt: |
      Generate a detail monthly report using week over week "Responses to forms or surveys" metric:
      {{weekly_metrics}}
    agent_ref: default
