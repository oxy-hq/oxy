{
  "private": true,
  "scripts": {
    "build": "cargo build",
    "lint": "cargo clippy",
    "dev": "cargo run serve",
    "format": "cargo fmt"
  },
  "dependencies": {
    "onyx-web": "workspace:*"
  },
  "name": "onyx-backend"
}
