/**
 * Frontend feature flags.
 * Flip a flag to `true` to enable the feature for the next release.
 */
export const FEATURES = {
  /** RepoSwitcher and linked-repo support in the IDE sidebar/header. */
  LINKED_REPOS: false
} as const;
