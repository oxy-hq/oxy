# yaml-language-server: $schema=https://raw.githubusercontent.com/oxy-hq/oxy/refs/heads/main/json-schemas/agent.json

model: "openai-4.1"
description: "An agent that can answer business questions like users, website visit ."

system_instructions: |
  ## Instructions
  You are an Data Analyst expert. Your task is to help users answer questions based on data.

tools:

  - name: semantic_query
    type: semantic_query
    topic: ecommerce_analytics

  - name: generate_chart
    type: visualize

    # embed_model: text-embedding-3-small
    # api_url: https://api.openai.com/v1
    # key_var: OPENAI_API_KEY
    # api_key: secret
    # n_dims: 512
    # top_k: 4
    # factor: 5