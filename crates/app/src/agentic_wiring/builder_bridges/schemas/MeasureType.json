{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Represents the type of a measure aggregation. `number` is a pass-through: `expr` must already contain its own aggregation.",
  "enum": [
    "count",
    "sum",
    "average",
    "min",
    "max",
    "count_distinct",
    "count_distinct_approx",
    "median",
    "custom",
    "number"
  ],
  "title": "MeasureType",
  "type": "string"
}