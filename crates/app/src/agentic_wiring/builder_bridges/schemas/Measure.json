{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Driver": {
      "description": "A driver relationship: a measure that influences this measure's value.\n\nSupports two modes: qualitative (`direction`/`strength`/`confidence`) and quantitative (`coefficient`/`form`/`intercept`/`lag`).",
      "properties": {
        "coefficient": {
          "description": "Marginal effect coefficient (quantitative mode).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "confidence": {
          "allOf": [
            {
              "$ref": "#/definitions/DriverConfidence"
            }
          ],
          "default": "medium",
          "description": "Confidence in the relationship."
        },
        "description": {
          "description": "Human-readable description of the relationship.",
          "type": [
            "string",
            "null"
          ]
        },
        "direction": {
          "allOf": [
            {
              "$ref": "#/definitions/DriverDirection"
            }
          ],
          "default": "unknown",
          "description": "Direction of the relationship."
        },
        "form": {
          "allOf": [
            {
              "$ref": "#/definitions/DriverForm"
            }
          ],
          "default": "linear",
          "description": "Functional form of the relationship."
        },
        "intercept": {
          "description": "Intercept term (quantitative mode).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "lag": {
          "description": "Lag in days before a change in the driver affects the target.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "measure": {
          "description": "Fully qualified measure reference (e.g., \"marketing.ad_spend\").",
          "type": "string"
        },
        "refs": {
          "description": "Links to supporting research, experiments, or documentation.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "strength": {
          "allOf": [
            {
              "$ref": "#/definitions/DriverStrength"
            }
          ],
          "default": "moderate",
          "description": "Strength of the relationship."
        }
      },
      "required": [
        "measure"
      ],
      "type": "object"
    },
    "DriverConfidence": {
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "type": "string"
    },
    "DriverDirection": {
      "enum": [
        "positive",
        "negative",
        "unknown"
      ],
      "type": "string"
    },
    "DriverForm": {
      "description": "Functional form of a quantitative driver relationship.\n\nThis mirrors `oxy_airlayer_compat::schema::models::DriverForm`, which is where the shape is actually defined (a basis plus a link, in `oxy_airlayer_compat::engine::response`). The copy exists because this crate parses `.view.yml` itself, so a form the engine accepts and this enum does not is a deserialization failure on a perfectly valid file — keep the two variant lists in step.\n\nNote the multi-term shapes (`quadratic`, `cubic`, `linear-log-quadratic`) cannot be given their coefficients through this crate's [`Driver`], which carries only the scalar `coefficient`. Declared here they are fit-only: the shape is honoured, the magnitude comes from history.",
      "enum": [
        "linear",
        "log-log",
        "log-linear",
        "linear-log",
        "quadratic",
        "cubic",
        "sqrt",
        "inverse",
        "linear-log-quadratic"
      ],
      "type": "string"
    },
    "DriverStrength": {
      "enum": [
        "strong",
        "moderate",
        "weak"
      ],
      "type": "string"
    },
    "MeasureFilter": {
      "description": "Represents a filter condition for measures",
      "properties": {
        "description": {
          "description": "Human-readable description of the filter",
          "type": [
            "string",
            "null"
          ]
        },
        "expr": {
          "description": "SQL expression for the filter condition",
          "type": "string"
        },
        "original_expr": {
          "description": "Original expression before variable encoding (if variables were used)",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "expr"
      ],
      "type": "object"
    },
    "MeasureType": {
      "description": "Represents the type of a measure aggregation. `number` is a pass-through: `expr` must already contain its own aggregation.",
      "enum": [
        "count",
        "sum",
        "average",
        "min",
        "max",
        "count_distinct",
        "count_distinct_approx",
        "median",
        "custom",
        "number"
      ],
      "type": "string"
    }
  },
  "description": "Represents a measure in the semantic model",
  "properties": {
    "description": {
      "description": "Human-readable description of what this measure represents",
      "type": [
        "string",
        "null"
      ]
    },
    "drivers": {
      "description": "Driver relationships: measures that influence this measure's value",
      "items": {
        "$ref": "#/definitions/Driver"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "expr": {
      "description": "SQL expression for the measure (required for most types, not for count)",
      "type": [
        "string",
        "null"
      ]
    },
    "filters": {
      "description": "List of filters to apply to the measure calculation",
      "items": {
        "$ref": "#/definitions/MeasureFilter"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "name": {
      "description": "Unique identifier for the measure within the view",
      "type": "string"
    },
    "original_expr": {
      "description": "Original expression before variable encoding (if variables were used)",
      "type": [
        "string",
        "null"
      ]
    },
    "samples": {
      "description": "Sample values or example outputs to help users understand the measure",
      "items": {
        "type": "string"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "synonyms": {
      "description": "Alternative names or terms that refer to this measure",
      "items": {
        "type": "string"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "type": {
      "allOf": [
        {
          "$ref": "#/definitions/MeasureType"
        }
      ],
      "description": "Type of measure aggregation"
    }
  },
  "required": [
    "name",
    "type"
  ],
  "title": "Measure",
  "type": "object"
}