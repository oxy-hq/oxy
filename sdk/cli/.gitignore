dist/
node_modules/
*.tgz
# Copied from the repo root by `prebuild` — one committed source of truth.
json-schemas/
# @generated by scripts/embed-assets.mjs at build time.
src/generated/
binaries/
# `bun build --compile` scratch files.
*.bun-build
