dist/

# Templates are pnpm. A stray npm/yarn lockfile or node_modules generated by
# running an install inside a template dir must never be committed and shipped
# to scaffolded apps (this is how a 60K package-lock.json once leaked in).
templates/**/node_modules/
templates/**/package-lock.json
templates/**/yarn.lock
