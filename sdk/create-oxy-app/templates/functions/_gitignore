node_modules

# build output — `out/` is what `oxy publish` ships
out
dist

# tsc / vite build state
*.tsbuildinfo

# foreign lockfiles — this kit is pnpm; commit pnpm-lock.yaml, not these
package-lock.json
yarn.lock

.DS_Store
.env
.env.local
